var util = require('./util.js')

/**
 * A layer of abstraction between the lobbies and the client API calls.
 * Handles multiple lobbies that exist. Should:
 * - know which lobbies each player belongs to
 * - process user actions and modify the appropriate lobby
 */
function LobbyManager() {
}